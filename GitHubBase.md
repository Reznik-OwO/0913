# GitHub基础

## GitHub网站

GitHub 的官方网站是 [GitHub]( https://github.com  "孙子点我") 。大量的企业级工程和开源工程在该网站云端托管、聚集海量项目。是软件研发工程师的工具网站。因为 GitHub 的服务器在**国外**。而国内网络访问国外服务器时，经常会遇到**“路不通”或“路太堵”**的情况。所以在访问GitHub网站时一般要用到加速器或者是VPN。

## 注册GitHub账号

一般利用QQ邮箱，163邮箱等注册GitHub账号。

## 在GitHub上查询资料等

模糊查询：按用户输入的关键词进行全站的查询,注意下面的语言标签

标签查询：查询样本时的格式：xxxx sample  查询资料时的格式：xxxx tutorial 

## Git软件

Git是管理代码的工具。点击***Git Bash Here***，就会显示**终端**（或者叫**命令行界面**）。在终端上输入Git命令，就可以指挥Git帮助我们管理代码。

## Git与GitHub的关系

一般用Git把本地代码上传（push）到 GitHub 上备份、分享或和别人协作。

## 仓库

仓库:是github中项目(工程)的存储单位,一个用户可以创建多个仓库,一般每个仓库存一个项目。

#### 仓库的核心板块

* 仓库
  * code(代码区)：是存放项目实际资源的地方。包含源代码、配置文件等。
    * `README.md`：是项目的说明书。作用是介绍项目是干嘛的、怎么用
    * `LICENSE`：是开源许可证文件。作用是规定别人能怎么用你的代码。常见类型有GPL3.0（限制严），MIT（限制最宽松）等
  * Issues(问题/问答板块)：是仓库的“提问与解决问题”区域。作用是便于让用户或开发者报告 Bug、提新功能建议、讨论问题。

## 分支

分支:资源的存储单位,一般一个仓库中有一个默认的主分支(Master),上传代码数据默认放到主分支中,仓库包含分支,一个仓库可以有多个分支,主分支只有一个。

## 设置Git的基本配置

* 在本地创建一个Git仓库

  * ```bash
    git init
    ```
  
* 查看配置信息

  * ```bash
    git config --list
    ```

* 配置用户名和邮箱

  * ```bash
	  git config --global user.name "用户名"
	  git config --global user.email "用户邮箱"
    ```

* 测试SSH连接

  * ```bash
	  ssh -T git@github.com
    ```

* 生成密匙/覆盖原有密匙（即私匙和公匙对）

  * ```bash
	  ssh-keygen -t rsa -C "用户邮箱"
    ```

* 将公匙添加到GitHub

  1. 通过文件路径找到公匙的内容并复制(.pub文件里是公匙)
  2. 打开 GitHub 网站，点击头像 -> **Settings**（设置）。
  3. 左侧菜单选择 **SSH and GPG keys**。
  4. 点击 **New SSH key**（新建 SSH 密钥）。
  5. 填写一个**密钥名**（随便起，方便自己识别）。
  6. 把刚才复制的公钥**粘贴**到 Key 输入框。
  7. 点击 **Add SSH key**（添加）。

* 给其他仓库的地址起别名

  * ```bash
	  git remote add 别名 其他仓库地址
    ```

* 删除地址别名

  * ```bash
	  git remote remove 别名
    ```

* 

## 本地Git仓库

当你对一个文件夹执行 `git init` 命令后，Git 会在**这个文件夹里面**创建一个隐藏的 **`.git` 文件夹**。

`.git` 文件夹里面存着：

- 所有的**历史版本**（提交记录）
- 所有的分支信息
- 配置信息
- 等等

这个`.git` 文件夹所在的文件夹就变成了**Git 仓库**。

当你改动了**Git 仓库**里的文件，保存，Git 会知道“你改了什么”。当你执行“提交（commit）”命令时，Git 会把当前的状态**存成一个历史版本**保存在`.git`文件夹里。之后你随时可以：

- 查看以前改了什么。
- 回退到任何一个历史版本。
- 开分支做改动。

  注：在ctrl+s后，Git不会额外存储。因为程序员在写代码时会频繁的按ctrl+s，如果这都保存会产生很多无意义的记录。

  对比普通文件夹：在普通文件夹内，你改了文件，在保存后，旧的版本就没了（除非你自己手动复制备份）。

## 把Git仓库里的文件上传到Github上的云端仓库的全过程

**工作区 (Workspace)**：在本地Git仓库内除 `.git` 以外的所有其他文件和文件夹。

**暂存区/缓存区 (Staging Area / Index)**：Git 的缓冲区，用于暂时存放``准备提交的修改。

`.git` 文件夹，保存了所有的版本历史记录。

**云端仓库 (Remote Repository / GitHub)**：GitHub中远程的仓库，用于团队协作和备份。

1. 把工作区的文件上传到本地仓库

   1. **暂存文件到暂存区：**

      * ```bash
      	git add file
        ```

   2. **暂存所有修改过的文件到暂存区：**

      * ```bash
        git add .
        ```

   3. **从暂存区和工作区同时删除文件：**

      * ```bash
        git rm file
        ```

   4. **将暂存区的文件还原到工作区（撤销修改）：**注：rm是普通的删除命令，不会通知暂存区

      * ```bash
        git restore file
        ```

   5. **删除暂存区中的文件（取消 add）：**

      * ```bash
        get restore --stage file
        ```

   6. **查看暂存区和文件的状态：**

      * ```bash
        git status
        ```

   7. **把暂存区的文件提交到.git文件夹中：**

      * ```bash
        git commit -m "提交说明"
        ```

2. 把.git文件夹中的文件上传到云端

   * ```bash
     git push 远程仓库的地址 master
     ```

3. 下载/克隆

   * 后续开源项目下载，**不以仓库为单位，而是以资源文件为单位**。可以以文件或文件夹为单位进行操作。

   * 把GitHub上的文件下载到本地仓库里的暂存区

     * ```bash
       git clone 地址
       ```

## 直接在 GitHub 网站上修改代码并提交，是导致 `git push` 被拒绝（即 `! [rejected]` 错误）的**最常见原因之一**

解决方法：

从远程 `origin` 的 `master` 分支获取最新代码，合并在源文件中。

```bash
git pull --rebase 仓库地址 master
```



手动解决源文件的冲突（源文件中会有两个版本的代码：一部分是本地修改，另一部分是云端修改）

```bash
git rebase --continue
```



直接保留云端的提交

```bash
git rebase --skip
```

## 仓库和分支的关系

**仓库里保存的提交历史构成一张有向无环图（DAG），分支只是这张图上的一个可移动指针。**

![very difficult](https://img.remit.ee/i/lvDGIGS4kzVq "very difficult!")
